services:
  - type: web
    name: greenbros-dashboard
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app
    envVars:
      - key: SECRET_KEY
        generateValue: true
      - key: DATABASE_URL
        fromDatabase:
          name: greenbros-db
          property: connectionString

databases:
  - name: greenbros-db
    plan: free
